{
  "name": "express-openapi-gen",
  "version": "0.0.5",
  "description": "A syntax analyzer that will attempt to generate an OpenAPI definition from your express API code.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon --watch index.ts --exec \"tsc --build\"",
    "build": "tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "readme": "README.md",
  "homepage": "https://github.com/johnW-ret/express-openapi-gen#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/johnW-ret/express-openapi-gen.git",
    "directory": "src"
  },
  "author": "retruate",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "typescript": "^5.1.3"
  }
}